# Internal governance + design notes — machine-local, never shipped to the public repo
CLAUDE.md
MEMORY*.md
AGENTS.md
.claude/
.agents/
COALWASH_BLUEPRINT.md
SENIOR-INCIDENT-AUDIT.md
LAB-ARCHIVE.md
# Manual pre-edit backups of the above (beside-the-file, hand-deleted at round close —
# NOT a scratchpad copy, see room MEMORY 2026-07-29). The exact-name entries above do
# not glob-match a "*.pre-edit.md" sibling; checked with `git check-ignore -v`, not assumed.
*.pre-edit.md
# machine-local agent-org process (supplier branches / assembly line) - not part of the shipped skill
ASSEMBLY-LINE.md

# Generated SkillSpector scan reports (provenance recorded in SECURITY.md; the raw report is not shipped)
skillspector-*

# IC lab scratch — throwaway prototypes/notes, never committed (this is the guard that still serves)
work/

# Department-head scratch — per-dispatch working material (dispatch prompts, raw JSON
# returns, throwaway red-first probe scripts, draft commit messages). Board #61
# look-then-merge (2026-08-06): every file present checked against the room's own
# durable record before ignoring — dispatch outputs and probe scripts are already
# narrated in full in MEMORY.md's dated entries, commit-msg drafts already landed as
# real commits (retrievable via `git log`), and the two pre-registered lab-walk
# predictions belong to the umbrella cw-labtest deputy's own memory-isolated volume,
# not this room's. Nothing here was load-bearing; nothing merged.
scratchpad/

# Internal design specs — process artifacts, not product (CLEAN-CLONE PRINCIPLE: a skill clone
# carries ONLY the skill). Added by MAIN 2026-07-27 after a resident measured that `git add -A`
# would stage two design specs into this PUBLIC repo; main's own dispatch had asserted this dir
# was already fenced, and `git check-ignore` on the DIRECTORY agreed while the FILES were exposed.
# Check the file, never the directory.
docs/

# Caretaker benches — git worktrees nested inside this repo (machine-local, never shipped).
# MUST stay ignored: an un-ignored nested checkout would be committed whole into a PUBLIC repo.
/CW-class-a/
/CW-class-b/

# CC platform-generated skill lockfile (machine-local, never ship)
skills-lock.json

# Transient test fixtures written BESIDE their source during a hermetic gate
# run (a re-export-hop probe, or a throwaway mutant copy for a red-first
# proof) — neither was gitignored before this (CWK-012 site 4): the
# pre-commit hook runs the full suite, so a broad `git add -A` mid-run can
# stage one — verify.mjs's LIBS roster already filters the hop prefix, but
# the git index does not, and this room has been bitten by it repeatedly.
# Board #106 already tried relocating the hop fixture OUT of scripts/lib/ and
# REVERTED it (a cross-drive path.relative broke Windows CI) — ignoring it in
# place, not moving it, is the cheap structural cure.
scripts/lib/.cw-reexport-hop-*.mjs
scripts/lib/*.mutant-*.mjs
